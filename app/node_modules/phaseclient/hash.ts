import Discussion from "phaseclient/discussion";
import Message from "phaseclient/message";
import TerrariaUser from "phaseclient/terrariauser";
import User from "phaseclient/user";

export interface UserHash { [userId: string]: User; }
export interface DiscussionHash { [discussionID: string]: Discussion; }
export interface BooleanHash { [id: string]: boolean; }
export interface MessageHash { [userId: string]: Message; }
export interface TerrariaUserHash { [displayName: string]: TerrariaUser; }
