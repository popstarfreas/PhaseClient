interface Channel {
    name: string;
    state: string;
    waitForAuth: boolean;
    SUBSCRIBED: boolean;
    PENDING: boolean;
    UNSUBSCRIBED: boolean;

    /* Events
    dropOut
    subscribe
    subscribeFail
    unsubscribe 
    subscribeStateChange
    */

    getState(): string;
    subscribe(channel: string): void;
    unsubscrribe(channel: string): void;
    isSubscribed(channel: string): boolean;
    publish(data: string, handler: (any) => void): void;
    watch(handler: (any) => void): void;
    unwatch(handler: (any) => void): void;
    watchers(): Array<(any) => void>;
    destroy(): void;
}

export default Channel;