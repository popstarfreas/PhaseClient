import Channel from "phaseclient/network/channel";

interface Socket {
        id: string;
        state: string;
        pendingReconnect: boolean;
        pendingReconnectTimeout: number;
        connectAttempts: number;
        authState: string;
        authToken: object | null;
        CONNECTING: string;
        OPEN: string;
        CLOSED: string;
        AUTHENTICATED: string;
        UNAUTHENTICATED: string;
        PENDING: string;

        // Events:
        // error(err)
        // connect()
        // disconnect()
        // connectAbort()
        // raw()
        // kickOut(message, channelName)
        // subscribe()
        // subscribeFail()
        // unsubscribe()
        // authStateChange()
        // authTokenChange()
        // subscribeStateChange()
        // subscribeRequest()
        // authenticate()
        // deauthenticate()
        // message()

        connect(): void;
        getState(): string;
        getAuthToken(): object;
        getSignedAuthToken(): object;
        disconnect([]): void;
        emit(event: string, data: string | object, callback?: () => void): void;
        on(event: string, handler: (any) => void): void;
        off(event: string, handler: (any) => void): void;
        send(data: string): void;
        publish(channelName: string, data: string, callback: () => void): void;
        subscribe(channelName: string | number): Channel;
        unsubscribe(channelName: string | number): void;
}

export default Socket;
