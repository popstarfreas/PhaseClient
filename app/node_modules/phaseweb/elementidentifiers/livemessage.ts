import Message from "phaseweb/elementidentifiers/message";

class LiveMessage extends Message {
    public static identifier(userId: string): string {
        return `#livetyping-user-${userId}`;
    }
}

export default LiveMessage;
