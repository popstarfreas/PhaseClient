import Config from "phaseclient/config";
import { TerrariaUserHash } from "phaseclient/hash";
import User from "phaseclient/user";

interface Client {
    terrariaUsers: TerrariaUserHash;
    systemName: string;
    configuration: Config;
    getPhaseUserByName: (username: string) => User | null;
    getPhaseUser: (userId: string) => User | null;
    username: string;
}

export default Client;
