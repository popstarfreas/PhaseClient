import CoreClient from 'phase/client';
import PageModel from 'phasemobile/pagemodel';
import Config from 'phase/config';

class Client extends CoreClient {
    constructor(configuration: Config) {
        super(configuration);
    }
}

export default Client;